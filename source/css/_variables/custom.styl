// 标题
$font-family-headings = Cambria, Georgia, sans

$font-family-base = "PingFang SC", Cambria, "Century Gothic", "Microsoft YaHei", Verdana, sans-serif

// 代码字体
$code-font-family = Menlo, Consolas, "PT Mono", Monaco, monospace

// 正文字体的大小
$font-size-base = 15px

// 代码字体的大小
$code-font-size = 13px
